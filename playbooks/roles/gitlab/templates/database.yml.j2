# PRODUCTION

production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: {{ gitlab.db.name }}
  pool: 10
  username: {{ gitlab.db.user }}
  password: "{{ gitlab.db.pass }}"
  host: "localhost"
  # socket: /tmp/mysql.sock
