production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: {{ gitlab_ci.db.name }}
  pool: 5
  username: {{ gitlab_ci.db.user }}
  password: "{{ gitlab_ci.db.pass }}"
  host: {{ gitlab_ci.db.host }}
  # socket: /tmp/mysql.sock

